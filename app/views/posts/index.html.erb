<!DOCTYPE html>
<html>
<head>
  <title>Postări</title>
</head>
<body>
  <br>
  <h1>Postări</h1>
  <br>
  <br>

  <div class="container">
    <div class="row justify-content-center">
      <% @posts.each do |post| %>
        <div class="col-md-8 mb-4 mx-auto">
          <div class="card">
            <div class="card-body">
            <h5 class="card-title">Postarea lui: <%= link_to post.user.email, user_path(post.user) %></h5>
              <%= link_to post.title, post, class: "card-title" %>
              <p class="card-text"><%= post.content %></p>
              <p class="card-text"><%= pluralize(post.comments.count, 'comment') %></p>
              <ul>
                <% post.comments.each do |comment| %>
                  <li>  <%= comment.user.email %>  a spus  "<%=comment.content %>" </li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
  
  <footer>
    <%= link_to 'New Post', new_post_path, class: "btn btn-primary bg d-grid gap-2 col-6 mx-auto footer fixed-bottom bg-primary" %>
  </footer>
</body>
</html>
</body>
</html>